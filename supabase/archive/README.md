# Archived migrations

One-off upgrade scripts applied while the product evolved. The live greenfield source of truth is `../schema.sql`.

These files remain for archaeology and for upgrading very old databases that never ran a given change. Prefer `schema.sql` for new projects.
